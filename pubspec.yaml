name: styfi
description: A Flutter project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase (Auth, Database, Storage)
  firebase_core: ^3.10.1
  firebase_auth: ^5.4.1
  cloud_firestore: ^5.6.2
  firebase_storage: ^12.4.1 # Added missing package
  
  # Supabase (Optional - kept if you use it, but Firebase Storage is main now)
  supabase_flutter: ^2.8.0 
  
  http: ^1.2.0
  image_picker: ^1.0.7
  cached_network_image: ^3.4.1
  google_fonts: ^6.1.0
  flutter_launcher_icons: ^0.14.3
  
  # Gallery Saver (Modern replacement)
  gal: ^2.3.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
